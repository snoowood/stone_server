.env

# SSL certs — production certs (Let's Encrypt) must not be committed
nginx/certs/fullchain.pem
nginx/certs/privkey.pem
# selfsigned.crt / selfsigned.key are local dev certs and are intentionally tracked

# Backup files
*.pgdump

# Local SQLite dev database
*.db
*.db-shm
*.db-wal

*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
/vendor/

# Per-milestone diff snapshots used as Codex review input (regenerated each round)
docs/m*_server_diff.patch
docs/m*_client_diff.patch
